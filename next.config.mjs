/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.mds.yandex.net",
      "www.fortunebuilders.com",
      "i.pinimg.com",
      // add more here
    ],
  },
};

export default nextConfig;
